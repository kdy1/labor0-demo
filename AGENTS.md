# Repository Instructions

- Use `pnpm` for package management and scripts.
- Keep TypeScript strict and write source code and comments in English.
- Preserve semantic HTML and keyboard and screen-reader accessibility.
- Use named exports for reusable modules.
- Run `pnpm typecheck`, `pnpm test`, and `pnpm build` before finishing.
- Open reviewable pull requests, but do not merge them.

