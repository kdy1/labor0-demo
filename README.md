# Labor0 release status demo

A small Rsbuild and React application used to demonstrate a scoped Labor0
coding task against a public repository.

## Development

```sh
pnpm install
pnpm dev
```

Run `pnpm typecheck`, `pnpm test`, and `pnpm build` before opening a pull
request.

